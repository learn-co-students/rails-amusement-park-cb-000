h1= @attraction.name
h2= button_to 'Go on this ride', rides_path, params: { name: @attraction.name, tickets: @attraction.tickets, nausea_rating: @attraction.nausea_rating, happiness_rating: @attraction.happiness_rating, min_height: @attraction.min_height }
br
p= link_to 'Edit Attraction', edit_attraction_path(@attraction) if current_user.admin